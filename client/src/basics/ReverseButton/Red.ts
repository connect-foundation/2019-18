import styled from 'styled-components';
import ReverseButton from '.';

const LongReverseButton = styled(ReverseButton)`
    width: 5rem;
    color: red;
    border: 2px red solid ;
`;

export default LongReverseButton;
