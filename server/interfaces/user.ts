export interface IUser{
    email: string,
    pwd: string,
    name: string,
    thumbnailUrl?: string,
    originUrl?: string,
}
export type User={
    email: string,
    pwd: string,
    name: string,
    thumbnailUrl?: string,
    originUrl?: string,
};
